defaults:
  - default_config

ExperimentConfig:
  initial_BC: true
  BC_finetuning: false
  ppo: false
  evaluate: false
  selfplay: false
  league_training: false

  model_path: BC_workerRush
  BC_model_path:
  exp_name: BC_workerRush_collect
  wandb_project_name: worker_focus

  resume: false
  prod_mode: false
  seed: 1
  render: true
  render_all: false

  # Collect only; BC loop starts at epoch 1, so epochs=1 skips training.
  newdata: true
  epochs: 1
  warmup_epochs: 1
  nurwins: false

  # Custom replay collection target.
  bc_expert_ai: workerRushAI
  bc_opponent_ais:
    - workerRushAI
    - mayari
    - lightRushAI
  bc_num_runs: 500
