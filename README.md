start the Projekt with "python main.py --config-name name". For selfplay insert selfplay_conf in place of name.

Selfplay is implemented in selfplay_only.py while ppo and BC are implemented in ppo.py and BC.py respectivly.

Evaluations against Bots are Implemented in evaluate.py and evaluations against other Agents is in selfplay_evaluate.py
